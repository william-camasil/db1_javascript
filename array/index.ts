// Usando Array<T>
let numeros: Array<number> = [1, 2, 3, 4, 5];

// Usando T[]
let frutas: string[] = ["maçã", "banana", "laranja"];

// Manipulando os arrays
numeros.push(6);
frutas.push("uva");

console.log(numeros);
console.log(frutas);
